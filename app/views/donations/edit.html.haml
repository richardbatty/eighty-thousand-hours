.row
  .span2
    .box.sidebar
      = render :partial => "shared/cause_donation_member_sidebar"

  .span10
    .box.left-border
      .page-header
        %h1 Edit a donation
      %p
        You can change the privacy settings of your donation using the form below.

      = render :partial => 'form', :locals => { :page => :edit }
