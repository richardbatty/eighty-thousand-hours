.box
  .page-header
    %h1 Blog posts by #{link_to @user.name, @user}
  
  - @posts.each do |post|
    .blog
      = render :partial => 'blog_posts/display_heading',
               :locals => { :post => post }

  %br
  = link_to '<i class="icon-user"></i> back to member profile'.html_safe, @user
