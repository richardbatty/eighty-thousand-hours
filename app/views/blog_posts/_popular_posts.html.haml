%h3 Popular posts
- @popular_posts.each do |post|

  = render partial: 'shared/sidebar_item',
           locals: { date: post.created_at,
                     content: "<span class='votes'>#{post.net_votes}</span><i class='icon-arrow-up'></i>#{link_to post.title, post_path( post )}".html_safe,
                     attribution: link_to_post_author( post ) }

= link_to "View all popular posts <i class='icon-circle-arrow-right'></i>".html_safe, "/blog?order=popularity"
%br
= link_to "View posts with most votes <i class='icon-circle-arrow-right'></i>".html_safe, "/blog?order=votes"
