.sidebar-item
  - if defined? content
    .content= content
  .attribution
    - if defined? attribution
      = "by ".html_safe + attribution
  -# Commented out dates to remove clutter    
  -# - if defined? date
  -#    on
  -#    %span.date= format_date(date)
