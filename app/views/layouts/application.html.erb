<!DOCTYPE html>
<html xmlns:fb="https://www.facebook.com/2008/fbml">
<head>
  <title>High Impact Careers</title>

  <link rel="shortcut icon" href="/favicon.ico" />
  <link rel="icon" href="/favicon.ico" type="image/ico" />

  <%= render :partial => "/shared/opengraph" %>

  <%= stylesheet_link_tag "highimpact" %>

  <!--[if lt IE 9]>
  <%= javascript_include_tag "html5" %>
  <![endif]-->

  <%= csrf_meta_tag %>

  <%= javascript_include_tag "misc" %>
  <%= javascript_include_tag "google-analytics" %>

</head>
<body>
  <% if flash %>
    <% flash.each do |key,value| %>
      <div class="<%= key %>"><%= value %></div>
    <% end %>
  <% end %>
  
  <div id="page_container"> 
      <header>
       <%= image_tag "hic_header_logo.png" %> 
       <%= render( {:partial => "/shared/menu"} ) %>
      </header>
      
      <%= render :partial => "/shared/user_nav"%>

      <section id='content'>
      <%= yield %>
      </section>

      <%= render :partial => "/shared/social_media" %>

      <footer>
        <%= render :partial => "/shared/footer" %>
      </footer>
  </div>
</body>
</html>
