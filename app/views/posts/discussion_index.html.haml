.page-header
  %h1 Discussion

.alert.alert-info
  This is a place for having public discussions. If you are signed in you can
  create a new post and others can comment on it. If a post is deemed suitable
  for promotion to the blog then an admin can do this. Please contact #{link_to 'editor@80000hours.org', 'mailto:editor@80000hours.org'} with any questions.

.controls
  - if can? :create, Post
    .user
      = link_to "New Post".html_safe, new_post_path, :class => 'btn btn-mini btn-success'
      = link_to "My Posts", posts_user_path(current_user), :class => 'btn btn-mini'
 
.blog
  - @posts.each do |post|
    = render partial: 'posts/discussion_header', locals: { post: post }
