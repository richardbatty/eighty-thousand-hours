%h3 Popular posts
- @popular_posts.each do |post|
  .sidebar-item
    .title
      %span.votes= post.net_votes
      <i class='icon-arrow-up'></i>
      = link_to post.title, post_path( post )
    .author
      = render :partial => 'posts/author_link', :locals => { :post => post }
